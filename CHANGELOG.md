# Changelog

## Unreleased

- Renamed the project and CLI to `ankglish`.
- Removed the generated legacy deck snapshot.
- Added the initial canonical models, TOML configuration, TSV validation, and
  deterministic offline build/export command.